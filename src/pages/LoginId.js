import React from "react";
import PageTemplate from "../components/PageTemplate/PageTemplate";
import LoginIdContainer from "../containers/LoginId/LoginId";

const Login = () => {
  return (
    <PageTemplate>
      <LoginIdContainer />
    </PageTemplate>
  );
};

export default Login;
